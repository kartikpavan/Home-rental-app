import Categories from "../components/Categories";
import Hero from "../components/Hero";

const HomePage = () => {
   return (
      <main>
         <Hero />
         <Categories />
      </main>
   );
};

export default HomePage;
